[package]
name = "shared_types"
version = "0.1.0"
edition = "2021"

[dependencies]
candid = "0.10"
serde = { version = "1.0", features = ["derive"] }
ic-cdk = "0.18"  # Required for audit module (caller(), time())
