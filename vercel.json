{
  "buildCommand": "pnpm run build",
  "outputDirectory": "build",
  "framework": "sveltekit",
  "installCommand": "pnpm install",
  "devCommand": "pnpm run dev",
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
